#include "other.eds"

void main () {
    int i;
    i = i+1;
    i = i-1;
    return;
}

int called (int a, char b, bool c, bool * d) {
    return 9;
}

void calling () {
    int assign;
    bool d;

    d = true;
    
    assign = called(9,'a',false, &d);
}
