#include "stdio.h"

void main () {
  int i, x[16], sum;

  sum  = 0;
  for (i = 0; i < 16; i++) {
    x[i] = i;
  }

  writeInteger(x[0]);
  writeChar('\n');
  writeInteger(x[5]);
  writeChar('\n');
  writeInteger(x[11]);
  writeChar('\n');
  return;

}
