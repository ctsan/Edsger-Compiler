#include "stdio.h"

// Don't use this in production! :P 
int fib(int n) {
    if ( n == 0 || n == 1 ) {
        return 1;
    } else {
        return fib(n-1)+fib(n-2);
    }
}

void main () {
    if (fib(5)==8) 
        writeString("Right\n");
    else
        writeString("Wrong\n");

    return;
}

