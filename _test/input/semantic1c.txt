char *l[10];
bool k[5+9*5], delta,epsilon;
double *z;

int my_fun(int***x, char y);

// This is allowed since we have different amount of params
int my_fun(int x, char y, int z);

int my_fun(int*** x, char y) {
	double l;
	double z[100];
	if (9) {
		myfirstlabel: for (;;) {
			break myfirstlabel;
		} 
	} else;
}

int anotherfunction() {
}
